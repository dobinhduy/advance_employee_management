import 'package:flutter/material.dart';

class SideMenuMoblie extends StatelessWidget {
  const SideMenuMoblie({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
